% =====================================================================
%  Format.tex — Shared preamble for QuantMethods book
%  Merged from: original Format.tex (Overleaf) + P1–P4 canonical preamble
%  Last updated: 2026-03-14
% =====================================================================

% ── Page geometry ─────────────────────────────────────────────────────
\usepackage[margin=2.5cm, headheight=14pt]{geometry}

% ── Core mathematics ──────────────────────────────────────────────────
\usepackage{amsmath, amssymb}
\usepackage[mathcal]{euscript}
\usepackage{bm}
\usepackage{latexsym}

% ── TikZ and PGF ──────────────────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc,
                decorations.pathreplacing, patterns,
                shapes.geometric, matrix, shapes, arrows}

% ── Tables ────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{threeparttable}
\usepackage{rotating}

% ── Lists ─────────────────────────────────────────────────────────────
\usepackage{enumitem}
\usepackage{paralist}          % inparaenum
\setlist[itemize]{leftmargin=1.5em, itemsep=2pt, parsep=0pt, topsep=4pt}
\setlist[enumerate]{leftmargin=2em,  itemsep=2pt, parsep=0pt, topsep=4pt}

% ── tcolorbox ─────────────────────────────────────────────────────────
\usepackage[most]{tcolorbox}
\tcbset{keyresult/.style={
    enhanced, colback=gray!8, colframe=black!50,
    boxrule=0.6pt, arc=3pt,
    left=6pt, right=6pt, top=5pt, bottom=5pt
}}
\tcbset{definition/.style={
    enhanced, colback=blue!4, colframe=blue!40,
    boxrule=0.6pt, arc=3pt,
    left=6pt, right=6pt, top=5pt, bottom=5pt
}}

% ── Graphics ──────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{caption}
\captionsetup{labelfont=normalsize, font=small}
\usepackage{subcaption}

% ── Typography ────────────────────────────────────────────────────────
\usepackage{microtype}
\usepackage{parskip}
\usepackage[T1]{fontenc}
\usepackage[scaled=0.85]{beramono}    % monospaced font for code

% ── Bibliography ──────────────────────────────────────────────────────
\usepackage[sectionbib, authoryear, round]{natbib}
\usepackage{chapterbib}
\bibliographystyle{econometrica}

% ── Index ─────────────────────────────────────────────────────────────
\usepackage{imakeidx}
\makeindex

% ── Hyperlinks ────────────────────────────────────────────────────────
\usepackage[bookmarks=true,
            citecolor=blue,
            urlcolor=blue,
            linkcolor=blue,
            colorlinks=true,
            bookmarksnumbered=true]{hyperref}

% ── Line numbers (draft mode) ─────────────────────────────────────────
\usepackage{lineno}

% ── Appendices ────────────────────────────────────────────────────────
\usepackage{appendix}

% ── Code listings ─────────────────────────────────────────────────────
\usepackage{listings}
\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{lstgray}{rgb}{0.4,0.4,0.4}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\lstset{
  backgroundcolor=\color{white},
  basicstyle=\scriptsize\ttfamily,
  breaklines=true,
  captionpos=b,
  commentstyle=\color{dkgreen}\textsl,
  frame=none,
  keepspaces=true,
  keywordstyle=\color{blue},
  language=R,
  numbers=left,
  numbersep=5pt,
  numberstyle=\tiny\color{lstgray},
  showstringspaces=false,
  stringstyle=\color{mauve},
  tabsize=2
}

% ── Misc formatting ───────────────────────────────────────────────────
\usepackage{pdflscape}
\addtolength{\skip\footins}{5mm}

% =====================================================================
%  Math operators and commands
% =====================================================================
\newcommand{\E}{\mathbb{E}}
\newcommand{\Var}{\operatorname{Var}}
\newcommand{\Cov}{\operatorname{Cov}}
\newcommand{\plim}{\operatorname{plim}}
\newcommand{\MSE}{\operatorname{MSE}}
\newcommand{\ind}{\perp\!\!\!\perp}   % independence symbol
\newcommand{\HRule}{\rule{\linewidth}{0.2mm}}
\def\bv#1{\mbox{\boldmath$#1$}}
\def\citeapos#1{\citeauthor{#1}'s (\citeyear{#1})}
\def\newblock{\hskip .11em plus .33em minus .07em}

% Equation sub-labelling (a, b within a numbered equation)
\newcounter{ind}
\def\eqlabon{%
  \setcounter{ind}{\value{equation}}\addtocounter{ind}{1}%
  \setcounter{equation}{0}%
  \renewcommand{\theequation}{\arabic{chapter}.\arabic{ind}\alph{equation}}}
\def\eqlaboff{%
  \renewcommand{\theequation}{\arabic{chapter}.\arabic{equation}}%
  \setcounter{equation}{\value{ind}}}
